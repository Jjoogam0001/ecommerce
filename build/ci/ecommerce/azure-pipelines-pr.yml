# Run pipeline when master or develop is changed
trigger:
  - master
  - develop

# Run pipeline whenever a PR is raised
pr:
  branches:
    include:
      - 'refs/heads/master'

stages:
  - stage: validate
    displayName: 'Validate and Test Go application'
    jobs:
    - template: validate-go.yml