global:
  scrape_interval:     15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'conflicts'

scrape_configs:
  - job_name: 'conflicts'
    scrape_interval: 5s
    static_configs:
      - targets: 
        - 'conflicts:80'
        labels:
          app: 'conflicts'
